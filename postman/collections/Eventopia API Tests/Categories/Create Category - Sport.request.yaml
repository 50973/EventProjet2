$kind: http-request
name: Create Category - Sport
method: POST
url: '{{baseUrl}}/api/categories'
order: 2000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{accessToken}}'
body:
  type: json
  content: |-
    {
      "name": "Sport"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 201", function () {
          pm.response.to.have.status(201);
      });

      pm.test("Response has category with id and name Sport", function () {
          const json = pm.response.json();
          const category = json.category || json;
          pm.expect(category).to.have.property("id");
          pm.expect(category.name).to.eql("Sport");
      });

      pm.test("Save categoryId2", function () {
          const json = pm.response.json();
          const category = json.category || json;
          pm.collectionVariables.set("categoryId2", category.id);
      });
    language: text/javascript
