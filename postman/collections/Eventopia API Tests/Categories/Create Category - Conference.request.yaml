$kind: http-request
name: 'Create Category - Conférence'
method: POST
url: '{{baseUrl}}/api/categories'
order: 3000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{accessToken}}'
body:
  type: json
  content: |-
    {
      "name": "Conférence"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 201", function () {
          pm.response.to.have.status(201);
      });

      pm.test("Response has category with id and name Conférence", function () {
          const json = pm.response.json();
          const category = json.category || json;
          pm.expect(category).to.have.property("id");
          pm.expect(category.name).to.eql("Conférence");
      });

      pm.test("Save categoryId3", function () {
          const json = pm.response.json();
          const category = json.category || json;
          pm.collectionVariables.set("categoryId3", category.id);
      });
    language: text/javascript
